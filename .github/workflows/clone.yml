name: GitHub Clone Count Update Everyday

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    # Feed the PAT to gh via GH_TOKEN rather than `gh auth login --with-token`,
    # which additionally demands a read:org scope this job has no use for.
    env:
      GH_TOKEN: ${{ secrets.SECRET_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - name: parse latest clone count
        run: |
          curl -sS --user "${{ github.repository_owner }}:${{ secrets.SECRET_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/${{ github.repository }}/traffic/clones \
            > clone.json

      - name: create gist and download previous count
        id: set_id
        run: |
          if gh secret list | grep -q "GIST_ID"
          then
              echo "GIST_ID found"
              echo "GIST=${{ secrets.GIST_ID }}" >> $GITHUB_OUTPUT
              curl -sSL https://gist.githubusercontent.com/${{ github.repository_owner }}/${{ secrets.GIST_ID }}/raw/clone.json > clone_before.json
              if grep -q '404: Not Found' clone_before.json; then
                echo "GIST_ID not valid anymore. Creating another gist..."
                gist_id=$(gh gist create clone.json | awk -F / '{print $NF}')
                echo $gist_id | gh secret set GIST_ID
                echo "GIST=$gist_id" >> $GITHUB_OUTPUT
                cp clone.json clone_before.json
                git rm --ignore-unmatch CLONE.md
              fi
          else
              echo "GIST_ID not found. Creating a gist..."
              gist_id=$(gh gist create clone.json | awk -F / '{print $NF}')
              echo $gist_id | gh secret set GIST_ID
              echo "GIST=$gist_id" >> $GITHUB_OUTPUT
              cp clone.json clone_before.json
          fi

      - name: merge into cumulative clone.json
        run: python3 scripts/clone_count.py

      - name: Update gist with latest count
        run: |
          content=$(sed -e 's/\\/\\\\/g' -e 's/\t/\\t/g' -e 's/\"/\\"/g' -e 's/\r//g' "clone.json" | sed -E ':a;N;$!ba;s/\r{0,1}\n/\\n/g')
          echo '{"description": "${{ github.repository }} clone statistics", "files": {"clone.json": {"content": "'"$content"'"}}}' > post_clone.json
          curl -s -X PATCH \
            --user "${{ github.repository_owner }}:${{ secrets.SECRET_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d @post_clone.json https://api.github.com/gists/${{ steps.set_id.outputs.GIST }} > /dev/null

          if [ ! -f CLONE.md ]; then
            shields="https://img.shields.io/badge/dynamic/json?color=success&label=Clone&query=count&url="
            url="https://gist.githubusercontent.com/${{ github.repository_owner }}/${{ steps.set_id.outputs.GIST }}/raw/clone.json"
            repo="https://github.com/${{ github.repository }}"
            {
              echo "# Clone count badge"
              echo
              echo '**Markdown**'
              echo
              echo '```markdown'
              echo "[![GitHub Clones]($shields$url&logo=github)]($repo)"
              echo '```'
              echo
              echo '**HTML**'
              echo
              echo '```html'
              echo "<a href='$repo'><img alt='GitHub Clones' src='$shields$url&logo=github'></a>"
              echo '```'
            } > CLONE.md

            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add CLONE.md
            git commit -m "chore: add clone count badge"
            git push
          fi

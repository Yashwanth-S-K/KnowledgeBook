# Local environment
.env
.venv/
venv/
env/

# Python caches / build output
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Generated app data — uploaded files, indices, KGs, notes, sessions
artifacts/

# MinerU runtime output (per-document parse cache, kept beside artifacts/)
output/

# Tectonic / LaTeX build artefacts
*.aux
*.log
*.out
*.toc

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
# macOS screenshot dumps that land in the repo root
截屏*.png
Screenshot*.png

# Per-machine Claude Code project config
.claude/

# Course-presentation deck (15 MB, kept locally; not for the repo)
ppt.pdf

# Local PDFs used as ad-hoc test inputs (course slides, sample uploads).
# Anything matching test-*.pdf or sample-*.pdf stays out of the repo.
test-*.pdf
sample-*.pdf

# Local screenshot working folder — canonical copies live in docs/screenshots/
pics/

# Playwright / browser-automation residue
.playwright-mcp/
playwright-report/
test-results/

# GitHub clone-count workflow scratch files
clone.json
clone_before.json
post_clone.json
